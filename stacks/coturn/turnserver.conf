# Minimal TURN/STUN relay for PairDrop over Tailscale.
# No TLS: all traffic stays inside the private Tailscale network.
listening-port=3478
listening-ip=0.0.0.0

min-port=49160
max-port=49200

fingerprint
lt-cred-mech
realm=homelab

log-file=stdout
no-cli
no-tls
no-dtls
